popular:
  manga: one-punch-man, record-of-ragnarock, vagabond, one-piece, fate-redline
roles:
  admin:
    password: ${ADMIN_PASSWORD}
    username: ${ADMIN_USERNAME}
  user:
    password: password
    username: username

spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
  rsocket:
    server:
      mapping-path: /rsocket
      port: 6969
      
logging:
    level:
        io:
         rsocket:
#           FrameLogger: DEBUG